/**
 * Where ComfyUI keeps its own files inside a vault — the project's workflows —
 * as plain files an agent can read and edit. Dotted and skipped by the scan: a
 * workflow is nib's configuration, not content, so none of it is drawn as a card.
 */
export const COMFYUI_DIRECTORY = ".comfyui";
